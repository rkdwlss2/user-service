spring:
  cloud:
    config:
      uri: http://localhost:8888
      name: user-service
      profile: test
#      profile: native
management:
  endpoints:
    web:
      exposure:
        include: refresh